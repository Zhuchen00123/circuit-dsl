# Rust 模拟电路仿真分析 DSL：完整开发提示词

> 使用方法：将本文交给编码代理，作为本项目的开发任务说明。要求代理先检查仓库并完成后端验证，再按阶段实现。本文定义目标与约束，不代表其中提到的第三方库已经通过本项目实测。
>
> 文档日期：2026-09-18。项目名称暂用 `circuit-dsl`，命令暂用 `cdsl`，可以在正式发布前统一修改。

---

## 1. 你的角色与任务

你是一名熟悉 Rust、编程语言实现、模拟电路和数值计算的工程师。请开发一个 **Rust 实现、具有 Ruby 风格语法的模拟电路仿真与分析 DSL**。

目标是让用户以简洁、可组合、可参数化的代码描述电路，执行工作点、直流扫描、交流小信号和瞬态分析，并读取、测量和导出结果。

这是一门独立语言，不要求兼容 Ruby，也不嵌入 Ruby 解释器。借鉴 Ruby 的代码块、符号、命名参数和自然的调用形式，同时提供明确的名称解析、量纲检查和错误诊断。

优先复用经过验证的 Rust 仿真内核，不要默认从零实现全部器件模型、非线性求解器和积分器。首选评估 Thevenin；保留其他后端接入能力。

交付的是可运行、可验证、有示例和文档的工程，而不只是设计草图、接口空壳或模拟输出。

## 2. 已确定的设计原则

1. Rust 负责语言工具链、核心数据结构、后端集成和命令行工具。
2. DSL 在展开阶段生成电路；数值仿真过程中不执行任意 DSL 脚本。
3. 项目拥有自己的 Circuit IR，通过适配层连接第三方仿真内核。
4. 电路定义、分析任务和结果表达式分离。
5. 数值必须具备量纲语义；单位转换在明确边界完成。
6. 首期展开完成后拓扑固定，参数扫描不允许隐式改变拓扑。
7. 不支持的分析、器件或参数必须明确报错，不能静默忽略或降级。
8. 用户看得到的错误应尽可能定位到 DSL 源码和层次实例。
9. 优先打通完整的小功能，再增加器件种类和语言特性。
10. 保留用户已有修改，遵循仓库 AGENTS.md，不进行未经授权的破坏性操作。

## 3. 产品范围

### 3.1 首个完整版本

- 文本 DSL 和 CLI。
- 电阻、电容、电感、独立电压源、独立电流源。
- 在后端验证通过后支持二极管。
- 参数、节点、地、参数化子电路及层次实例。
- 有限循环和条件展开。
- 工作点 OP、直流扫描 DC、交流 AC、瞬态 TRAN。
- 电压、电流探针，实数和复数结果。
- 单参数扫描。
- 基础测量：最大值、最小值、平均值和 RMS。
- CSV 和带元数据的 JSON 输出。
- 可定位的语法、语义、量纲和后端错误。

以上按阶段交付，不要求第一轮同时实现。所有对外声称支持的能力必须有真实执行路径和验证用例。

### 3.2 后续扩展

- MOSFET、BJT、受控源、行为源、开关、互感。
- SPICE 网表导入与导出。
- 噪声、灵敏度、Monte Carlo、优化和参数拟合。
- 波形绘图、编辑器语法支持、LSP。
- 浏览器和 WebAssembly。
- Verilog-A 或其他模型接入。
- 自研 Rust 仿真内核。

不要为这些后续功能提前创建大量空 crate、插件协议或服务。

### 3.3 首期不做

- 完整 Ruby 兼容、类体系、元编程或 method_missing。
- 任意脚本文件读写、网络访问或外部命令执行。
- 仿真过程中动态添加和删除器件。
- 分布式仿真、桌面原理图编辑器、云服务。
- 无条件承诺兼容全部 SPICE 方言和厂商模型。
- 默认引入 LLVM、JIT 或动态插件 ABI。

## 4. 先做仓库检查与后端技术验证

在大规模编码之前：

1. 检查现有文件、AGENTS.md、Rust 工具链、Git 状态和构建环境。
2. 核实候选后端当前发布版本、许可证、实际 API、依赖及目标平台支持。
3. 完成一个独立、最小的后端调用示例。
4. 记录实际运行命令、结果、限制和选型结论。
5. 根据结果确定后端，再实现 DSL 到后端的适配。

### 4.1 优先评估 Thevenin

已查到的文档将它描述为 Rust SPICE 仿真器，并以 `cirq_ir::Circuit` 为主要输入。请现场核对，不得凭本文编造结构体字段或函数签名。

验证内容：

- 能否在当前 Windows 环境编译运行；其他平台能力另行验证。
- 能否从 Rust 数据结构构造电路，而不必先生成 Cirq 源码。
- OP、AC、TRAN 和 DC 的调用接口是否满足要求。
- 节点、电流、频率、时间及复数结果的表达方式。
- 源、模型、初值、容差和分析选项如何传递。
- 工作点和收敛失败是否能返回可识别的错误。
- 是否存在全局状态、线程安全或实例隔离限制。
- crate 版本是否相互匹配，公开接口是否稳定。

### 4.2 后备方案

- Spice21：评估基础器件、OP、AC、TRAN 和嵌入接口。
- ngspice：若原生 Rust 后端不能满足目标，可作为可选外部后端或测试参考；必须明确其内核为 C。
- faer：仅在需要自研内核时评估稀疏线性代数能力。
- diffsol：仅在方程形式匹配时评估瞬态求解能力，不把通用微分方程库等同于电路仿真器。

未经评估，不直接采用存在已知内存安全问题的 Rust FFI 包装库。若使用外部进程，应用参数数组调用，避免将用户表达式拼接成 shell 命令。

### 4.3 后端准入用例

至少实际验证：

1. 电阻分压的 OP。
2. RC 电路的瞬态响应。
3. RLC 或 RC 电路的 AC 幅相响应。
4. 二极管电路的非线性求解；可作为第二阶段准入项。

对结果和解析解或可信参考值进行比较，不能只检查进程退出码。验证失败时记录原因，修复或选择后备方案，不伪造结果。

## 5. 目标 DSL 语法

以下是语言设计目标，不能假设这些接口已经存在。实现前形成一份简短语法规范，使所有示例保持一致。

```ruby
circuit :rc_filter do
  param :r, default: 1.kohm
  param :c, default: 100.nF

  node :vin, :vout

  voltage_source :input, p: :vin, n: :gnd,
    dc: 0.V,
    ac: 1.V,
    waveform: pulse(low: 0.V, high: 1.V,
                    delay: 1.us, rise: 10.ns,
                    fall: 10.ns, width: 5.us,
                    period: 10.us)

  resistor :r1, p: :vin, n: :vout, value: r
  capacitor :c1, p: :vout, n: :gnd, value: c
end

experiment :response, circuit: :rc_filter do
  op
  ac from: 10.Hz, to: 10.MHz, points_per_decade: 50
  tran stop: 30.us, max_step: 50.ns

  save v(:vin), v(:vout), i(:input)
end
```

### 5.1 基础语法规则

- UTF-8 源文件，`#` 表示行注释。
- 标识符首期使用 ASCII 字母和下划线，后续字符可含数字。
- `:name` 表示符号；字符串与符号具有不同类型。
- `do ... end` 表示代码块。
- 调用支持命名参数；省略括号仅支持语法明确的语句调用。
- 表达式中的函数调用使用括号，减少歧义。
- 括号内以及逗号后允许续行，明确换行和语句终止规则。
- 支持整数、浮点数、科学计数法、布尔值、字符串、数组。
- 支持 `+ - * /`、一元正负号、比较和布尔运算，明确优先级。
- 不允许未声明变量自动变成函数调用或节点。
- 数值溢出、非有限值及非法量纲运算给出明确诊断。

### 5.2 名称与引用

- 节点、器件、参数、电路定义和实验使用明确的命名空间。
- 在相应语境中将符号解析成 NodeId、DeviceId 等强类型引用。
- `:gnd` 为保留的全局参考地。
- 节点原则上显式声明，防止拼写错误自动生成浮空节点。
- 重复定义报错，错误信息同时指出两处源码位置。
- 实例内部节点隔离；使用层次路径区分，如 `top.filter.r1`。
- 层次探针可采用 `v("filter.out")`，具体路径语法在规范中固定。

### 5.3 参数、子电路与实例化

```ruby
subcircuit :lowpass, ports: [:input, :output, :ground] do
  param :r, default: 1.kohm
  param :c, default: 100.nF

  resistor :r1, p: :input, n: :output, value: r
  capacitor :c1, p: :output, n: :ground, value: c
end

circuit :two_stage do
  node :vin, :mid, :out
  voltage_source :src, p: :vin, n: :gnd, dc: 1.V

  instance :stage1, of: :lowpass,
    ports: { input: :vin, output: :mid, ground: :gnd },
    params: { r: 2.kohm, c: 47.nF }

  instance :stage2, of: :lowpass,
    ports: { input: :mid, output: :out, ground: :gnd }
end
```

- 上例需要字典字面量和多行命名参数语法，不能只在文档中支持。
- 参数覆盖遵循确定顺序：默认值 → 实例或实验覆盖 → 扫描点覆盖。
- 参数依赖可构成 DAG；循环依赖必须报错并显示依赖链。
- 子电路递归实例化首期禁止，显示调用链。
- 端口缺失、多余端口、未知参数和量纲不匹配均报错。
- 展开结果需保留原始定义和实例化位置。

### 5.4 循环与条件

- 只支持展开期有限循环及条件，先采用明确的 `for ... in ... do ... end` 形式。
- 限制展开步数、最大器件数和层次深度；上限可配置。
- 动态生成的器件名称必须确定且唯一。
- 不支持无限循环或任意递归函数。
- 参数若影响条件、循环次数或连线，则标记为拓扑参数。
- 首期扫描拓扑参数时明确拒绝；后续可通过逐点重新展开支持。

## 6. 类型与单位系统

至少包含以下内部类型：

```text
Quantity<Dimension>
Bool / String / Symbol
NodeRef / DeviceRef
CircuitDefinition / CircuitInstance
Waveform / AnalysisSpec / SignalExpression
```

要求：

- 内部数值统一采用 SI，保留用户输入位置及必要的显示单位信息。
- 明确支持 V、A、ohm、F、H、s、Hz 和常用前缀。
- 大小写敏感：`m` 为 milli，`M` 为 mega。
- 明确 `1.kohm`、`1.uF`、`1.us` 等合法写法。
- 同量纲可加减，乘除按量纲指数推导；比较要求量纲兼容。
- 电阻、电容、电感和波形参数按照参数契约检查。
- 首期普通 R/L/C 要求正值；零值或负值不能偷偷替换成很小的正数。
- 单位符号是受限语法，不是任意对象方法调用。
- 需要无量纲参数的地方不能隐式接受带单位值。

区分三类表达式：

1. 展开期表达式：参数、常量、循环、条件。
2. 结果表达式：`v(:out)`、电压比值、RMS 等。
3. 求解期表达式：行为源等，后续单独实现，要求导数和不连续点处理。

禁止直接在求解器内部调用普通 DSL 解释器。

## 7. 分层架构与目录

```text
DSL source
   ↓
Lexer / Parser / AST
   ↓
Name resolution / Type and dimension checks
   ↓
Elaboration: parameters, subcircuits, loops
   ↓
Project-owned Circuit IR + AnalysisPlan
   ↓
Backend adapter
   ↓
Simulation engine
   ↓
Result dataset → expressions / measurements / export
```

建议 Cargo workspace：

```text
crates/
  circuit-core/        # IR、ID、量纲、分析计划、共享诊断类型
  circuit-dsl/         # 词法、语法、语义检查、展开
  circuit-backend/     # 后端契约与已选后端适配器
  circuit-results/     # 数据集、表达式、测量、导出
  circuit-cli/         # CLI
examples/
tests/fixtures/
docs/
```

可以合并尚未形成独立职责的小模块。不要让 core 依赖第三方后端，不要为了未来自研提前建立空的 solver/models crate。

### 7.1 Circuit IR

至少表达：

- CircuitId、NodeId、DeviceId、ModelId。
- 节点表、器件实例、端口连接、已解析参数、模型引用。
- 层次路径及展平映射。
- 源码 Span、定义位置、实例化位置。
- 元件和源的方向约定。

电路 IR 不包含第三方矩阵槽位、临时牛顿状态或后端对象指针。首期只覆盖项目已经支持的器件，避免设计无法落实的万能器件协议。

### 7.2 分析计划

AnalysisPlan 独立于电路结构，包含分析任务 ID、输入参数、探针、扫描轴和输出选项。

后端边界示意，具体签名由实现确定：

```rust
trait SimulationBackend {
    fn capabilities(&self) -> BackendCapabilities;
    fn validate(&self, circuit: &Circuit, plan: &AnalysisPlan)
        -> Result<(), BackendError>;
    fn run(&mut self, circuit: &Circuit, plan: &AnalysisPlan)
        -> Result<SimulationResults, BackendError>;
}
```

要求：

- 能力声明包括分析、器件和必要的选项范围。
- 后端名称、版本和实际求解设置进入结果元数据。
- 适配层维护 ID 和后端名称映射。
- 后端不支持的选项必须报错，不能悄悄忽略。
- 不强制后端满足未经核实的 Send/Sync 假设。
- 多个后端未来可并存，但首期只实现一个可用后端。

## 8. 分析与结果语义

### 8.1 OP

- 返回直流工作点。
- 电容和电感的直流语义由仿真引擎正确处理。
- 数值失败不能被包装成成功的空结果。

### 8.2 DC

- 首期支持单个源值或非拓扑参数扫描。
- 明确扫描起点、终点、步长、包含端点的规则。
- 校验步长非零、方向正确和最大点数。
- 每个点都有参数坐标、状态及结果关联。
- 默认遇到失败停止；可在后续提供显式的继续策略。

### 8.3 AC

- 区分源的 DC 值、AC 小信号幅相与瞬态波形。
- 默认围绕直流工作点线性化。
- 返回复数响应和频率轴。
- 频率范围必须正且有效，扫频点数规则固定。
- 增益 dB 仅接受无量纲比值，采用 `20 * log10(abs(ratio))`。
- 相位单位、主值范围以及是否展开相位必须明确。

### 8.4 TRAN

- 明确起止时间、最大内部步长和输出采样间隔的区别。
- `max_step` 不能被解释成保证等间隔输出。
- 后端仅支持部分时间选项时，暴露真实能力。
- 初期采用默认工作点初始化；跳过工作点等选项仅在后端验证后开放。
- 脉冲需检查周期、脉宽和边沿时间；不连续点处理能力必须经过验证。

### 8.5 统一结果结构

结果至少包含：

```text
experiment_id / analysis_id / scan coordinates
axis: scalar | time | frequency | parameter
signals: identity, unit, real-or-complex values
status and diagnostics
backend and solver metadata
```

- OP、AC 和 TRAN 可以保存不同探针集合。
- 同一实验执行多种分析时，结果按 analysis_id 区分，不能覆盖。
- 电压 `v(a, b)` 定义为 Va - Vb，`v(a)` 默认相对地。
- 电流正方向按器件 p → n 定义，并在适配层验证符号。
- 无法直接获取的器件电流不得伪造；只允许使用经过验证的推导。
- 非均匀时间轴上的平均值和 RMS 应基于时间积分，不直接取样本算术平均。
- 结果表达式在分析上下文内验证，例如标量 OP 没有时间 RMS。
- CSV 的复数列拆为 real/imag；JSON 明确保留单位、轴与元数据。
- 规定非有限数值的导出策略，不能输出非法 JSON。
- 结果大小必须有可控上限；流式导出仅在后端支持时实现。

## 9. 错误诊断与安全边界

诊断格式示例：

```text
error[E_DIMENSION]: resistor.value 需要电阻量纲，实际为时间
  --> examples/filter.cdsl:8:48
   |
 8 | resistor :r1, p: :vin, n: :out, value: 10.ms
   |                                                ^^^^^
   = instance: top.filter.r1
   = expected: ohm
   = received: s
```

必须覆盖：

- 非法字符、缺失 end、错误参数和表达式。
- 未声明节点、重复器件、未知电路和错误端口。
- 参数循环依赖与子电路递归。
- 单位错误及无效器件值。
- 展开限额、扫描点数与结果规模限额。
- 后端能力缺失、收敛失败、奇异系统和运行错误。

只有确定可识别的问题才给出确定诊断。例如“浮空节点”不能仅通过图上是否有任意连接来判断，电容路径并不等同于直流参考路径。无法定位时保留原始后端信息，不编造故障器件。

若支持 include 或模型文件，应限定在显式允许目录并检查解析后的路径；首期可暂不支持。DSL 本身不具有通用文件和网络权限。

## 10. 如确需自研数值内核

只有现成内核验证不满足目标，或任务另行要求时才进入本节。应先记录工程成本和替代方案。

以改进节点分析和下式作为设计基础：

```text
d(q(x, p))/dt + f(x, p) - b(t, p) = 0
```

- x 包括节点电压和必要的支路电流。
- q 表达电荷、磁通等储能项。
- 按 DAE 处理，不预设为普通显式 ODE。
- 电路语义 IR 与数值系统布局分离。
- 预先分配未知量和稀疏结构，器件绑定矩阵位置。
- 内层迭代避免名称查找、拓扑创建和重复结构分配。
- 试探状态与已接受状态分开；时间步拒绝可完整回退。
- 相对/绝对容差需考虑不同未知量单位和残差尺度。
- 建立收敛判断、阻尼/限幅、初始化和失败诊断。
- 优先评估 faer 的稀疏 LU，不假设电路矩阵对称正定。
- 先完成线性 OP，再实现固定步长 RC/RLC 瞬态，之后增加非线性与自适应积分。

自研内核应与独立解析解和外部参考引擎交叉验证。若产品使用某个引擎，不能只把同一引擎的另一条调用路径当作独立正确性依据。

## 11. CLI 与开发体验

目标命令示例：

```text
cdsl check examples/rc_filter.cdsl
cdsl run examples/rc_filter.cdsl --experiment response --out results/
cdsl capabilities
cdsl --version
```

- `check` 至少执行语法、语义、量纲和展开检查；声明是否包含后端能力检查。
- `run` 真正执行分析并输出结果路径和简洁摘要。
- `capabilities` 列出当前可用后端及已实现能力。
- 失败返回非零退出码；普通诊断与数据输出通道分开。
- 输出路径规则明确，不意外覆盖输入源码。
- 普通用户默认看到电路相关错误，详细内部信息通过 verbose 选项提供。
- 不承诺尚未实现的命令；README 和实际 CLI 同步。

## 12. 分阶段实施与验收

### 阶段 0：发现与选型

交付：`docs/backend-evaluation.md`、最小真实后端示例、依赖和平台记录。

验收：分压 OP 和至少一个动态或频域用例运行成功，列出限制、版本与证据；不满足则明确记录阻塞与替代方案。

### 阶段 1：最小闭环

实现：基础语法、节点、电阻、电压源、单位、OP、IR、适配器、CLI 和结果导出。

验收：用户从 `.cdsl` 文件运行分压电路，获得正确电压和方向一致的电流；错误节点与单位给出可定位诊断。

### 阶段 2：基础动态分析

实现：电容、电感、源波形、AC、TRAN、复数和时间轴结果。

验收：RC 瞬态与解析响应匹配，AC 幅相正确，RLC 用例通过；明确内部步长与输出采样语义。

### 阶段 3：语言组合能力

实现：参数、子电路、层次实例、有限循环、条件展开和单参数扫描。

验收：多实例节点隔离、参数覆盖与量纲正确，循环依赖和递归可诊断，扫描结果不会互相污染。

### 阶段 4：非线性与分析体验

实现：经验证的二极管支持、结果表达式、基础测量和完整错误处理。

验收：二极管工作点或整流示例通过对照，失败有真实原因，平均值与 RMS 对非均匀采样计算正确。

### 阶段 5：交付整理

交付：README、语言规范、架构、后端说明、示例、测试说明和限制清单。

验收：干净环境按 README 可运行示例，文档与实现一致，没有将 TODO 或模拟数据标为已实现。

## 13. 测试策略

### 13.1 前端

- 词法与语法正反例，尤其是单位字面量、续行和 do/end。
- 优先级、作用域、重复定义和未知名称。
- 单位计算、参数覆盖和循环依赖。
- 子电路展平、端口绑定、实例节点隔离和稳定 ID 映射。

### 13.2 仿真正确性

- 分压：`Vout = Vin * R2 / (R1 + R2)`。
- RC 阶跃：在匹配初值和理想阶跃假设时比较指数响应；有限上升沿不能直接套用不匹配的解析解。
- RC AC：比较 `H(jω) = 1 / (1 + jωRC)` 的实虚部或幅相。
- RLC：比较明确拓扑下的解析传递函数或可信参考输出。
- 电流方向、单位和守恒关系。
- 非线性电路：与独立参考结果比较。
- 不同时间/频率网格比较时说明插值和对齐方法。

容差要依据问题与求解设置制定，避免所有场景统一使用宽松阈值。测试应检查数值，而不是仅检查字段数量、文件存在或成功退出。

### 13.3 工程验证

- `cargo fmt --check`。
- 适用于工程的 `cargo clippy` 和 `cargo test`。
- 至少一次 CLI 端到端执行。
- 对当前环境无法执行的验证明确标注未验证，不声称跨平台通过。
- 不为低价值的实现细节堆积测试；优先覆盖接口契约、数值正确性和错误边界。

## 14. 必需文档与最终交付

至少提供：

```text
README.md
docs/architecture.md
docs/language.md
docs/backend-evaluation.md
docs/testing.md
examples/voltage_divider.cdsl
examples/rc_filter.cdsl
examples/rlc.cdsl
examples/diode_rectifier.cdsl    # 对应阶段实现后加入
```

README 包含实际安装步骤、最小示例、CLI 用法、已支持功能、已知限制及后端依赖。

阶段总结和最终回复需要说明：

1. 实现了什么、用户如何运行。
2. 采用的后端及版本。
3. 实际执行过哪些验证，结果如何。
4. 哪些功能尚未实现或未经验证。
5. 重要文件路径和必要后续工作。

不要把计划、文档中描述的目标或第三方 README 的声明当成本项目已完成的能力。

## 15. 执行方式

- 默认使用简体中文沟通，代码标识符使用清晰英文。
- 先发现、验证，再做关键架构决策；合理的小决策自主完成。
- 维护一份简短实施计划，阶段完成后更新状态和验证证据。
- 不因为可以继续设计而长期停留在设计阶段，应尽快交付可运行闭环。
- 需要重大范围改变时说明证据与影响；不能静默把 Rust 原生目标改为外部 C 引擎。
- 只有在用户或仓库规范授权时使用子代理；分配写任务时必须明确互不重叠的文件范围。
- 保护用户已有修改，避免无关重构和不必要依赖。
- 不上传、发布或部署项目，除非另有明确授权。
- 所有完成声明都应与实际代码和运行证据对应。

## 16. 官方资料入口

以下是选型研究入口，实施时核实当前版本与具体接口：

- Thevenin 仓库：https://github.com/cramt/thevenin
- Thevenin API：https://docs.rs/thevenin/latest/thevenin/
- Spice21：https://github.com/dan-fritchman/Spice21
- faer：https://docs.rs/faer/latest/faer/
- faer 稀疏求解：https://docs.rs/faer/latest/faer/sparse/linalg/solvers/
- diffsol：https://docs.rs/diffsol/latest/diffsol/
- ngspice 共享库：https://ngspice.sourceforge.io/shared.html

## 17. 开始执行

现在请检查仓库，给出简短实施计划，完成阶段 0 的后端技术验证，然后按上述阶段推进。不要仅重复本文；请产出实际文件、可运行代码和验证结果。遇到客观阻塞时准确说明，不编造接口、运行输出或完成状态。
