# 第 3 轮 Agent Team 执行提示词：结果表达式与测量

请作为主代理，在 `F:\codexprojects\dsl000` 完成本轮开发。本文明确授权你使用多代理团队实施、测试与独立复核。请产出可运行代码和验证证据，不只返回计划。

## 1. 先读与当前状态

按顺序读取：

1. 当前仓库适用的 AGENTS.md 与协作规范。
2. `docs/round2-acceptance-and-round3-plan.md`：本轮计划与验收标准。
3. `docs/review-evidence/round2/implementation-summary.md`、`final-gate.md`：上一轮实际交付与限制。
4. `docs/language.md`、`docs/architecture.md`：当前语义与模块边界。
5. 本文涉及的实际代码，再决定局部实现细节。

历史基线是 457 个 workspace 测试通过，Clippy/fmt 通过；独立 tran_contract 12/12、breakpoint_study 14/14 契约检查通过，但粗步长的 NOT-MET 配置仍保留。开工时核对现状，不直接继承通过结论。

仓库已经有六个产品 crate、CLI、REPL、Thevenin 后端和独立 `_probe`。大量上一轮修改可能尚未提交，必须保留。不要 reset、clean、重新脚手架化或覆盖工作区，也不要自动 commit/push。

## 2. 本轮唯一主目标

将已有结果表达式求值能力连接到 DSL、CLI、REPL、测量和派生信号导出。

现有入口：

- `crates/circuit-results/src/expr.rs`：结果表达式求值器。
- `crates/circuit-core/src/plan.rs`：分析与测量计划，当前 MeasureRequest.target 是 Probe。
- `crates/circuit-dsl/src/{ast.rs,parser.rs,elaborate.rs}`：语法与语义入口。
- `crates/circuit-session/src/execute.rs`：统一执行、测量、输出视图。
- `crates/circuit-results/src/resample.rs`：求解后输出重采样。

先确认文件与符号仍存在。复用已有求值器，不能复制一套仅供 CLI 使用的表达式引擎。

本轮不做参数 DAG、新器件、GUI、LSP、后端替换或自研求解器。不要重复上一轮完整问题研究；只重跑受影响的瞬态回归。

## 3. 目标语法与功能

下面是待实现语法，不是当前能力声明。语法允许必要的小调整，但主代理必须先冻结规范，并统一全部示例和测试。

```ruby
experiment :response, circuit: :rc_filter do
  ac from: 100.Hz, to: 100.kHz, points_per_decade: 40

  derive :gain, expr: v(:vout) / v(:vin)
  derive :gain_db, expr: gain_db(v(:vout), v(:vin))
  measure :peak_gain, max: abs(v(:vout) / v(:vin))
end

experiment :power, circuit: :rc_filter do
  tran stop: 1.ms, max_step: 100.ns, output_interval: 10.us
  measure :avg_power, avg: v(:vin, :vout) * i(:r1)
end
```

实现范围：

- 电压/电流探针、无量纲常量、括号、一元正负号和四则运算。
- abs 和 gain_db；额外函数仅在现有求值器已有且不扩大任务复杂度时加入。
- 实数与复数结果、单位推导、命名派生信号导出。
- 多分析显式绑定，例如 `analysis: :ac1`。
- 自动收集表达式需要的探针，不要求用户手动 save。
- 文件模式与 REPL 的相同语义。

首期 derive 不能引用另一个 derive，不能访问其他分析的数据。重名派生信号、与保存信号的名称碰撞应在定义阶段拒绝；避免导出时覆盖列。

## 4. 先冻结的架构契约

### 4.1 表达式与类型

共享计划层表达已解析的探针、运算和分析归属，results 负责运行时求值。禁止 circuit-core 反向依赖 circuit-results。

可以引入独立共享表达式类型或转换层，但不要将前端 AST 直接泄露到后端求解器。保存源码 span，使单位、名称和绑定错误可以定位。

冻结以下内容后才允许多个 worker 并行实现：

- 表达式 IR 与分析绑定字段。
- Probe 的身份、去重和自动依赖集合。
- derive/measure 的结果类型与单位。
- 显式输出信号和内部求值信号的区别。
- 静态验证和运行时诊断的返回类型。

### 4.2 分析绑定与兼容

- 单分析实验中，新增 derive/复合 measure 可省略 analysis。
- 多分析实验中，新表达式要求明确绑定；无法唯一确定时报告歧义，不能根据运行成功与否偷偷换到别的分析。
- 旧的直接探针 measure 不带 analysis 时，保留已文档化的 TRAN → AC → DC → OP 选择顺序，并固定同类型声明顺序规则。显式 analysis 优先。
- 元数据记录实际使用的分析身份。
- 参数 DC 扫描的最终数据集名称可能不同于普通 dc1。先核对映射，用语义分析 ID 绑定，不能只用结果名称猜测。无法支持的组合必须在静态检查或明确能力验证阶段拒绝。

### 4.3 执行顺序

```text
静态解析/类型与绑定检查
  → 收集每项分析所需探针
  → 后端求解，得到原始网格
  → 原始网格上计算派生表达式与测量
  → 选取用户请求的输出信号
  → 输出重采样
  → CLI/REPL 展示与 CSV/JSON 导出
```

- 不把 output_interval 传回求解器。
- 非线性表达式先求值后重采样，不能先插值原始信号再计算非线性表达式。
- 改变输出间隔不得改变测量值。
- 保持现有原始数据/输出视图边界，不因新增功能复制整个仿真流程。
- 同一分析共享探针去重，但不同分析不共享错位数组。

### 4.4 错误契约

- 未知探针、量纲错误、非法绑定、复数直接 max/min、非时间分析的 avg/rms 尽可能在 check 报错。
- 对复数取 max/min 必须先显式转换为实数，如 abs。
- gain_db 要求分子分母量纲相同；结果作为 dB 展示值，说明内部无量纲与显示标签之间的关系。
- 除零和 gain_db 的零幅值输入，本轮默认返回表达式运行错误，包含分析、信号和样本坐标。不要用任意 epsilon 将非法值变成有限数。
- 显式请求的测量不能悄悄消失。当前 evaluate_measures 的 Err 后 continue 逻辑需要改为区分“不适用分析”和“所选分析执行失败”。
- 运行时表达式失败不得导出一份貌似完整的成功结果；明确退出码与已有文件处理方式。
- 大数组广播、临时结果、派生信号和输出点数遵守现有资源限制；不扩大到完整运行取消机制。

## 5. Agent Team 编排

明确授权主代理分批启动子代理，最多 1 个主代理 + 15 个子代理，按独立任务实际需要使用。不要为了填满并发槽位重复劳动。

所有命名子代理必须从空白上下文启动，使用 `fork_turns="none"`。任务正文必须包含仓库路径、目标、相关背景、可读输入、精确写集、验收条件和禁止事项。

建议角色：

| 角色 | 职责 | 写集规则 |
|---|---|---|
| 前端 explorer | 只读核对 parser/AST/展开入口与兼容规则 | 无源码写入 |
| 数值 explorer | 只读核对现有 expr/measure 的类型与错误行为 | 无源码写入 |
| 会话 explorer | 只读核对分析 ID、扫描拼接、REPL 和导出 | 无源码写入 |
| 主代理 | 冻结 IR/接口、公共文件修改、整合与最终审核 | core、manifest、共享入口默认独占 |
| 前端 worker | derive/复合 measure 语法、类型与绑定、依赖探针收集 | 精确分配 frontend 文件 |
| 结果 worker | 求值器适配、复数/单位/错误行为 | 精确分配 results 文件 |
| 会话 worker | 公共接口稳定后接入执行、测量与输出 | session 文件，避免与主代理重叠 |
| QA worker | CLI/REPL 真实流程、数值与错误回归 | 新的独立集成测试文件 |
| 文档 worker | 实现冻结后更新语言规范与用户示例 | 指定 docs，README 由主代理统一管理 |
| 独立审核者 | 代码/数值/接口兼容/最终证据复核 | 只读，报告由主代理归档 |

约束：

- 建立文件所有权表；同一文件同时只有一个写入者，不按函数划分并行写入权。
- 每个 worker 必须被告知：“你不是唯一在代码库工作的代理，不能撤销其他人的修改。”
- 接口冻结前只做探索、设计和独立测试规划，不让多个 worker 各自发明公共类型。
- 测试、构建和全局格式化由主代理协调，禁止多人同时 fmt workspace。
- 审核针对冻结快照；修复后重新检查受影响范围。
- 主代理复核子代理实际 diff 和运行证据，不直接接受“完成”的口头结论。
- 不创建用户可见的新会话代替子代理。

## 6. 阶段与完成门槛

### 阶段 0：基线和设计

检查现有工作区、保留修改、核对测试基线。写出简短设计契约和所有权表，明确兼容策略。

### 阶段 1：最小闭环

先打通单分析的 `measure :x, max: abs(v(:out)/v(:in))`，包括无 save 时自动读取探针。真实运行成功后再扩展 derive、显式绑定和导出。

### 阶段 2：完整本轮能力

实现派生信号、gain_db、功率测量、多分析绑定、诊断、CLI/REPL 与重采样一致性。

### 阶段 3：独立验证与交付

数值复核、代码审核和真实 CLI/REPL QA，修复发现后同步文档并完成最终门禁。

本轮不止于阶段 1，必须完成计划中的全部本轮能力或明确列出经过核实的阻塞。

## 7. 必须通过的场景

1. RC 频率响应：截止频率处 gain≈0.5−0.5j、幅度≈0.70710678、gain_db≈−3.01029996。确保频率点匹配；不能在邻近点套用截止点参考值。
2. 电阻瞬时功率与独立计算 `v²/R` 相符，方向约定一致。
3. 细/粗 output_interval 下测量一致，派生波形输出点数按网格契约变化。
4. 无显式 save 时表达式有效，隐式原始探针不会意外全部出现在导出中。
5. 多个同类型分析正确绑定；未知 analysis_id 和新增表达式的歧义被拒绝。
6. 旧直接探针 measure 的兼容优先级不回归。
7. 单位错误、除零、零幅值 dB、不可用电流、未知节点、复数直接归约均给出确定诊断。
8. 派生名称冲突被拒绝；CSV/JSON 保留实/复数、单位和分析身份。
9. CLI 与 REPL 同一输入得到一致结果和错误；表达式失败不能被吞掉。
10. 单参数 DC 扫描路径正确绑定或明确拒绝不支持的组合，不能漏跑分析或错配坐标。
11. 原有瞬态边沿、非法间隔、断点和原始网格测量回归保持通过。

数值容差依据解析解、操作误差和现有配置制定，禁止测试失败后无依据放宽。无需重复写大量仅验证实现细节的测试。

## 8. 门禁与证据

执行：

```text
cargo test --workspace
cargo clippy --workspace --all-targets -- -D warnings
cargo fmt --all -- --check
```

根据实际改动运行独立 `_probe` 与格式检查；workspace 检查不覆盖 `_probe`。若只清理其局部警告，运行对应程序确认不改变数值。

新增证据保存在 `docs/review-evidence/round3/`，至少有：

- baseline.md：本轮基线、用户已有修改。
- design-contract.md：语法、IR、绑定、错误和导出契约。
- team-board.md：任务、依赖与文件所有权。
- acceptance.md：真实命令、数值、退出码和 QA 场景。
- final-summary.md：实际交付、独立审核、限制和下一步。

不要覆盖 round2 历史报告。日志可存 target 下，但持久摘要应足够复现。测试计数报告实际值，不将历史 457 当作固定目标。

## 9. 本轮顺手收尾

- 核对 README 的测试计数，避免 456/457 等旧数字继续漂移。
- 清理 breakpoint_study 的局部 unused_mut/未使用字段警告，不使用全局 allow 掩盖。
- 保留第三方断点精度和长仿真资源方面的已知限制，不声称本轮已修复。

## 10. 开始执行

现在检查仓库，组织第一批空白上下文的只读代理，同时由主代理完成基线核查。冻结接口后推进独立写集的实现，完成验证、审核和交付。

合理的局部决策自主完成；重大范围变更先说明依据。不要自动提交、推送、发布、部署或改变全局工具链。请持续推进直到本轮完成，不能只回计划或反复询问是否继续。
