# The sources are LF and stay LF: this keeps `cargo fmt --check` and any diff
# on a Windows checkout identical to a Linux one.
* text=auto eol=lf

# Binary-ish files git should not try to normalise.
*.png binary
*.pdf binary
