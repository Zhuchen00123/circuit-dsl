# 第 4 轮 Agent Team 执行提示词

你在 `F:\codexprojects\dsl000` 实施 Rust 电路 DSL 第 4 轮。请组织 Agent Team 完成工作，不停留在分析或计划。默认简体中文，保留当前所有用户修改，不自动切换到会遗漏未提交交付的 checkout。

首先读取仓库协作规范、`docs/round3-review-and-round4-plan.md`、`docs/review-evidence/round3/final-summary.md` 和相关代码。以本轮复核计划为范围依据，旧轮次提示词只作历史资料。

总目标有两个串行阶段：

1. 修复 R4-01 非法表达式中间值被掩盖、R4-02 量纲溢出 panic、R4-03 导出警告丢失；建立正式回归测试并独立验收。
2. 阶段 1 全部通过后，实现参数 DAG、前向引用、环诊断、覆盖后依赖重算与拓扑依赖传递，在 check 阶段拒绝间接拓扑扫描。

不要把两阶段同时投入前端实现。可以在阶段 1 并行安排 DAG 只读探索。主代理先冻结公共接口和作用域语义，再给 worker 分配互不重叠的写集；公共 core 类型由主代理负责。命名子代理从空白上下文启动，任务正文自包含，注明文件所有权及不得撤销他人改动。团队按独立任务扩展，不为凑代理数量拆分共享文件。配置独立 QA 与只读 reviewer，主代理复核后才宣布阶段完成。

先亲自复现计划中的输入：sqrt(-1)、min(sqrt(-1),2)、1e308*1e308、128 个 v(:vin) 相乘。修复不能只检查最终输出，不能用 epsilon、饱和、跳过样本或 catch_unwind 掩盖错误。量纲检查覆盖 DSL 可达的乘除和现有幂路径；至少定向验证 debug/release。导出诊断使用构造 Dataset 的独立测试，避免依赖非法表达式仍能流入导出。

阶段 2 开始前，在 `docs/review-evidence/round4/design-contract.md` 固定作用域身份、覆盖优先级、有效定义图、环和未知名称诊断、REPL 失败状态、拓扑依赖传播规则。兼容现有文档化行为；若发现冲突，先从代码和测试取证，在契约中明确选择与影响。

验收必须包含独立数值参考、失败路径、CLI/REPL 一致性及旧功能回归，不能只统计新增测试。按计划执行验收矩阵，运行工作区测试、Clippy 和 fmt。失败时继续定位并修复；若存在无法解决的外部阻塞，如实记录已经完成和未完成的阶段，不宣布全轮通过。

将复现、测试命令、实际结果和独立审核保存到 `docs/review-evidence/round4/`，同步语言/架构/REPL 文档，给出最终交付总结。不要新增 GUI、新器件族、第二后端或重写求解器。不要自动提交、推送或发布。本任务的完成标准是计划内两阶段都通过独立验收，或明确报告未解决的阻塞及其证据。
