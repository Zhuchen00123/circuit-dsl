# 大规模 Agent Team 执行提示词：Rust 电路 DSL 数值验收与证据修正

> 将本文完整交给主代理执行。本文明确授权主代理为本轮任务建立多代理团队、分配局部实现与独立复核任务，并在共享工作区中分批并行推进。
>
> 本文是一份执行提示词，不表示团队已经启动，也不表示任何待办修复已经完成。

## 1. 任务目标

你是本项目的主代理与技术负责人。请组织一个可扩展到 **1 个主代理 + 15 个子代理** 的 Agent Team，完成现有 Rust 模拟电路 DSL 的数值验证修正、针对性回归和文档同步。

使用实际环境允许的并发数。建议先运行 6–8 个独立任务，再按依赖补充执行、QA 与复核角色；最多使用 16 个同时活动的代理，包括主代理。不要为了填满槽位重复审计或制造没有独立产出的任务。

**本轮必须完成：**

1. 修正错误的“浮空节点”验证用例和结论。
2. 重新建立 RC 瞬态的匹配解析解、全区间误差统计和步长/容差验证。
3. 把关键判断变成可失败的自动化断言，并覆盖产品适配器路径。
4. 补充已有接口上的非零相位转换验证，不借机扩大 DSL 语法范围。
5. 同步后端评估、测试说明、README 和受影响注释。
6. 完成独立代码审核、数值复核、真实 CLI QA 和最终统一验收。

最终交付应包含实际代码、可复现命令、真实结果和明确限制。不能止步于计划，也不能用测试数量代替数值正确性。

## 2. 项目上下文

工作目录：`F:\codexprojects\dsl000`。若实际环境路径不同，以当前仓库根目录为准。

项目已经存在，不要重新脚手架化：

```text
crates/circuit-core       # IR、单位、诊断与连接检查
crates/circuit-dsl        # 解析、语义与展开
crates/circuit-backend    # Thevenin 适配器和扫描
crates/circuit-results    # 结果、表达式、测量和导出
crates/circuit-session    # 会话与共享执行路径
crates/circuit-cli        # 文件模式 CLI 和 REPL
_probe                   # 独立后端验证工程，排除在产品 workspace 之外
examples
docs
```

已知基线：

- 现有后端为 Thevenin 0.5.0，直接通过 Rust 结构构造电路。
- 上次复核执行 `cargo test --workspace --quiet`，389 个测试通过，含 1 个 doc-test。
- 这是历史基线，不代表当前检出状态仍完全相同；开工时必须重新核实。
- 原产品目标见 `RUST_CIRCUIT_DSL_PROMPT.md`。
- 本轮问题与证据见 `docs/prompt-review.md`。
- 后端历史证据见 `docs/backend-evaluation.md`。

## 3. 范围约束

本轮聚焦现有实现的可信度和交付完整性。

保留现有 CLI、REPL、多个分析实例、示例和用户修改。不得把原提示词当作覆盖已有功能的理由。

本轮不实施：

- 新的仿真引擎、后端替换或依赖大版本升级。
- 参数 DAG、任意结果表达式的 DSL 接入。
- 新器件、绘图、LSP、GUI、WebAssembly 或云服务。
- 为支持非零相位测试而新增整套 DSL 相位语法。
- 发布、上传、部署、自动提交或推送 Git。

参数 DAG 与结果表达式可以由只读代理形成下一轮任务说明，但不得混入本轮实现。已有代码若与本文假设不同，以检查结果调整计划，不强行套用过时文件结构。

## 4. 主代理必须遵守的协作协议

### 4.1 主代理所有权

主代理负责总体规划、架构和跨模块决策、视觉任务（本轮通常无）、写入所有权表、共享文件修改、整合、最终复核和交付。

子代理提交的“通过”只是待复核证据。主代理必须检查改动和验证结果，不能直接转述为最终结论。

### 4.2 空白上下文启动

- 所有命名子代理必须从空白上下文启动：若工具支持，设置 `fork_turns="none"`。
- 每个任务正文必须自包含，包括仓库路径、目标、背景、允许写入文件、禁止事项、输入证据、验收标准和报告路径。
- 不能只发“按上文继续”或依赖子代理知道当前对话。
- 使用可用的 explorer 做只读检索；worker 或 deepseek_worker 做边界明确的执行；角色不可用时采用等价角色，不伪造模型或工具能力。
- 不指定未经要求的模型；沿用环境配置。

### 4.3 文件所有权

- 多个写任务只在文件写集完全不重叠时并行。
- 每次派工在 `docs/review-evidence/team-board.md` 登记写集、状态、依赖和验收负责人。
- 同一个文件只能有一个当前写入者。不同函数、不同代码段也不构成安全的并行写集。
- 所有 Cargo.toml、Cargo.lock、根 README、共享模块入口和公共 API 默认由主代理独占。
- 新增独立集成测试文件可由不同 worker 分别拥有；命名须唯一，确认没有 `autotests = false` 等配置限制。
- 需要超出写集时，子代理先发消息说明必要性，由主代理转交所有权或自行集成。
- 不允许代理通过复制整份旧文件覆盖当前版本。
- 每个子代理都必须被告知：**你不是唯一在代码库中工作的代理，不要撤销其他人的修改。**

### 4.4 共享构建与运行资源

- 只读检索和数学推导可以大规模并行；写代码和数值程序运行按依赖及资源约束调度。
- 由主代理协调完整 workspace 测试、格式化、Clippy，禁止多个代理同时执行整库格式化。
- 局部测试可申请执行时段；Cargo 锁等待不是测试失败。
- 若需要独立 target 目录，先评估磁盘和编译开销，由主代理统一安排，不为每个代理默认复制构建缓存。
- 每个实验使用不同的输出目录，禁止覆盖彼此的 CSV、JSON、日志。
- 不修改全局 Cargo、Rust 工具链或用户环境设置。

### 4.5 审核冻结与生命周期

- 代码审核、数值复核和 QA 开始前，明确审查快照及文件版本；工作区未提交时可用文件 SHA-256 清单固定被审查版本。
- 审核期间停止对相关文件写入；修复后让受影响的审核者复核新增 diff。
- 完成的子代理及时回收。若没有关闭工具，标记完成且不再派发无关任务，不虚报已关闭。
- 不创建用户可见的新 Codex 任务来模拟子代理；使用当前任务的多代理工具。

## 5. 已知问题与具体技术要求

### 5.1 浮空用例

`_probe/src/bin/robustness.rs` 中某个被称为 float 的用例包含：

```text
地 ─ 电压源 1 V ─ 节点 a ─ 电阻 1 kohm ─ 节点 b（开路输出）
```

b 通过电阻和电压源具有电势参考，开路时电流为零、b 为 1 V 是合理结果。不能据此声称引擎使用 gmin 掩盖了无参考节点。

需要覆盖三个不同场景：

1. 合法开路输出：应接受，并断言 b 电压及支路电流。
2. 完全孤立的电阻网络：无参考电势，检查前端诊断并观察后端原始行为。
3. 仅通过电容耦合的节点：检查直流工作点所需参考路径。

区分“前端基于项目规则拒绝”和“后端返回什么”。后端若给出有限值，不能仅据此归因于某个内部算法；需要源码或实验依据。

不能因为旧证据有误就删除现有连接检查，也不能宣称图上存在直流路径就足以保证所有电路可解。矛盾电压约束、非线性无解等仍可能需要求解器诊断。

### 5.2 RC 瞬态

历史验证使用 R = 1 kohm、C = 100 nF、1 ps 上升沿，在五个时间点附近比较理想阶跃解，阈值为 0.01 V。历史报告将约 0.00195 V 偏差归因为有限边沿和采样对齐。

这不是已确认的求解器 bug，而是需要重新建立证据的归因问题：边沿/时间常数约为 1e-8，且原代码已经使用实际返回时间求参考值。

新验证应采用有延迟的有限斜坡。令 u 为相对斜坡起点的时间，τ = RC，斜坡持续 T，最终幅值 V0：

```text
u < 0:
  y(u) = 0

0 <= u <= T:
  y(u) = V0/T * (u - τ + τ*exp(-u/τ))

u > T:
  y(u) = V0 + (y(T) - V0)*exp(-(u-T)/τ)
```

要求：

- 独立推导并验证公式、单位、边界连续性、极限与初始条件。
- 接近零时用稳定计算形式，必要时使用 expm1 或级数，不能让参考值本身产生明显消减误差。
- 脉冲下降沿放在验证区间之外，模拟至少覆盖上升结束后的 5τ。
- 使用实际返回的全部有效时间点，记录最大误差、发生时间、点数和分析配置。
- 明确后端返回时间轴是否严格递增、是否存在重复点，并按实际契约处理，不能悄悄删除失败样本。
- 分别改变 max_step 与容差，避免同时改变多个因素后无法解释效果。
- 检查传入选项实际到达后端，不把未生效的设置当作实验变量。
- 不要求误差每一个点都严格单调下降，但必须报告趋势、异常及证据边界。
- 若发现初始化或积分异常，定位最小复现；不得仅放宽断言通过验收。

初始验收目标沿用原提示词 §17：

```text
abs(actual - expected) <= atol + rtol * abs(expected)

OP voltage: atol 1e-9 V, rtol 1e-6
OP current: atol 1e-12 A, rtol 1e-6
AC voltage: atol 1e-8 V, rtol 1e-4
TRAN voltage: atol 1e-5 V, rtol 1e-3
```

这是新一轮目标，不是历史测试既定阈值。调整阈值必须附数值依据并经过独立复核。若达不到，保留失败或明确标记限制，不得伪造通过，也不默认改写第三方求解器。

### 5.3 相位、结果和文档

- 在已有 Rust IR/适配接口上测试非零 AC 相位和正弦相位，验证度与弧度转换以及符号约定。
- 正弦瞬态对照必须匹配初始状态；不能不加说明地用稳态公式对照包含启动过程的全区间。
- 核对复数实虚部和相位，而不只核对幅度。
- 更新 README 中已过时的分压例子；当前电路结果应由实际参数和执行结果确定。
- 参数 DAG、结果表达式入口和相位 DSL 尚未完成的部分明确列为后续工作。

## 6. 团队编排

下表是角色池，不要求同时启动全部角色。主代理在派工前必须验证文件存在并固定写集；若某任务没有独立有效产出，不启动。

| 角色 | 模式 | 独立职责 | 默认写入范围 | 依赖 |
|---|---|---|---|---|
| A01 仓库取证 | explorer / 只读 | 环境、当前差异、实际入口与构建命令 | 仅返回报告 | 无 |
| A02 浮空证据审计 | explorer / 只读 | 核对拓扑、旧证据、前端规则与现有测试 | 仅返回报告 | 无 |
| A03 数学参考审计 | 只读 | 独立推导 RC 斜坡解、误差尺度与稳定求值 | 仅返回报告 | 无 |
| A04 后端接口取证 | explorer / 只读 | 当前依赖源码、初始化、容差、步长、相位接口 | 仅返回报告 | 无 |
| A05 浮空用例实现 | worker | 修正 robustness 正反例及失败退出行为 | `_probe/src/bin/robustness.rs` | A02 |
| A06 瞬态验证实现 | worker | 匹配解析解、全区间统计、受控配置实验 | `_probe/src/main.rs` | A03、A04 |
| A07 拓扑产品回归 | worker | 合法开路与真正无参考网络的产品路径测试 | 新文件 `crates/circuit-dsl/tests/reference_path_regression.rs` | A02 |
| A08 瞬态适配回归 | worker | 产品 IR → 后端 → 结果的数值验证 | 新文件 `crates/circuit-backend/tests/transient_reference_regression.rs` | A03、A04 |
| A09 相位适配回归 | worker | 非零 AC/正弦相位与复数结果验证 | 新文件 `crates/circuit-backend/tests/phase_regression.rs` | A04 |
| A10 文档同步 | worker | 基于已冻结证据更新评估、测试文档 | `docs/backend-evaluation.md`、`docs/testing.md` | 实现与实测完成 |
| A11 数值独立复核 | 只读 | 审核参考公式、误差统计、容差与结论 | 仅返回报告 | 实现冻结 |
| A12 代码审核 | 只读 | 审核 diff、失败路径、越界改动与回归风险 | 仅返回报告 | 实现冻结 |
| A13 CLI QA | QA 执行 | 真实 CLI 检查、运行、输出和失败退出码 | 仅独立 QA 输出目录 | 实现冻结 |
| A14 契约差异分析 | explorer / 只读 | 参数 DAG、结果表达式与文档差异的下一轮清单 | 仅返回报告 | 无，可低优先级 |
| A15 最终门禁复核 | 只读 | 核对实现、数值审核、代码审核与 QA 证据 | 仅返回报告 | A10–A13 完成 |

主代理独占：README、提示词、team-board、最终报告、所有 manifest/lockfile、公共入口与必须发生的生产代码修复。

如发现生产代码缺陷，主代理评估后可将精确文件写集转交专门 worker，但必须先收回该文件其他写入权，并重新安排相关审核。禁止测试 worker 顺手修改整个适配层。

## 7. 分批执行与门槛

### Wave 0：基线与派工

主代理读取 AGENTS.md、提示词、复核记录、Git 状态与现有测试配置，建立 team-board，保存初始差异清单。

启动 A01–A04，可同时启动 A14。主代理检查当前构建和基线测试，读取关键代码；不要闲等所有只读任务结束。

**G0：**确认文件、输入证据、写集和基线；用户已有改动已识别，未知环境差异已记录。

### Wave 1：边界明确的并行实现

依据只读取证结果分批启动 A05–A09。每个任务依赖满足后即可启动，不必等待无关任务。A05 与 A06 不共享文件；三个新测试文件各有唯一写入者。

主代理负责跨文件协调和源码注释修正，组织局部测试时段。数学参考结论由主代理统一转发给相关 worker，避免复制不同假设。

**G1：**修正后的用例与回归测试运行完成，失败会返回非零；没有删除原有断言、忽略测试或偷偷放宽阈值。

### Wave 2：冻结、实测与独立复核

冻结候选实现并记录文件哈希，启动 A11、A12、A13。主代理执行完整 workspace 测试与 `_probe` 命令，集中保存真实输出。

Reviewer 不修改生产文件；发现问题交回主代理，再由原所有者修复。受影响的审核必须在修复后重新通过。

**G2：**数值复核与代码审核无未处理阻断项，CLI QA 覆盖成功与失败路径，所有结果对应相同候选版本。

### Wave 3：文档、最终门禁与交付

证据稳定后启动 A10。主代理同步 README，更新完成状态和下一轮任务，启动 A15 做最终证据复核。

最终检查按修改影响执行，避免对未变化内容无意义重复大规模测试。数值、代码或配置有新变化时，重跑相关检查。

**G3：**文档与实测一致，最终审核对应最终 diff，完成清单和未验证项明确。

## 8. 自包含子代理任务模板

主代理必须为每个子代理填写并发送以下信息：

```text
角色与任务名：<唯一名称>
仓库：F:\codexprojects\dsl000
模式：<只读 / 局部写入 / QA>

背景：这是已有 Rust 电路 DSL，使用 Thevenin 0.5.0。
本轮目标是修正浮空用例与 RC 瞬态验证证据，不重建项目。
你从空白上下文开始，必须读取列出的文件后执行。

你的目标：<一个明确且有界的结果>
输入文件与证据：<具体路径、上游报告结论、必要公式>
允许写入：<精确文件；只读时写“无”>
禁止写入：<公共文件、manifest、其他测试、产品模块等>
依赖：<已完成的输入或当前阻塞>
验收：<命令、数值性质、失败行为、报告内容>
测试运行安排：<可立即执行或向主代理申请时段>

你不是唯一在代码库中工作的代理。保留其他人的修改，不能回滚、重置或覆盖
他人的代码。发现需要越出写集时先报告，不能自行扩大范围。
不得改写阈值掩盖失败，不得把计划当作实测，不得提交或推送 Git。
完成后返回：修改文件、关键 diff 说明、实际命令与退出码、结果摘要、
证据位置、剩余风险、是否触及依赖或公共 API。
```

主代理汇总只读报告到各自独立的证据文件；不要让所有子代理同时修改 team-board 或同一个总结文档。

## 9. 证据组织

建议结构：

```text
docs/review-evidence/
  team-board.md
  baseline.md
  numerical-reference.md
  backend-contract.md
  implementation-summary.md
  numerical-review.md
  code-review.md
  cli-qa.md
  final-gate.md
  final-summary.md
```

尽量精简报告，不复制大量重复日志。原始命令输出和生成数据可放在独立的本地验证产物目录；是否加入版本管理由主代理根据仓库约定决定。最终摘要应保留足够证据，即使大日志未提交也能复现。

每份审核记录至少包含：

- 审查范围和对应版本或哈希。
- 实际阅读/执行了什么。
- 发现、严重程度和文件位置。
- PASS / NEEDS_FIX / BLOCKED 之一。
- 未验证项和限制。

不得以“上游 README 声称支持”“worker 说通过”“输出看起来合理”替代证据。

## 10. 最终验证清单

主代理按实际工程配置确认命令后执行：

```text
cargo test --workspace
cargo fmt --all -- --check
cargo clippy --workspace --all-targets -- -D warnings
```

`_probe` 是独立工程，不会被上述 workspace 命令自动覆盖。必须另外检查其 Cargo.toml，运行对应 probe/robustness 二进制及适用的格式、测试检查，记录退出码。

至少保留以下行为证据：

- 合法开路输出的 OP 与电流方向。
- 真正无参考网络的前端诊断，以及后端原始返回行为。
- RC 有限斜坡全区间对照及误差发生位置。
- 步长和容差分别变化时的结果。
- 非零 AC 相位与正弦相位的适配正确性。
- 一个真实 DSL 文件的 CLI 成功执行和结果导出。
- 一个真实非法 DSL 文件的失败诊断与非零退出码。
- 既有 REPL 测试和其他 workspace 测试没有回归。

历史 389 个测试是基线，不是最终固定数量；新增测试后报告真实数量。若基线因用户修改发生变化，说明当前基线，不为凑数字删改测试。

## 11. 阻塞、失败与范围控制

- 工具、依赖或环境阻塞先收集证据并继续无依赖任务，不立即让所有代理停工。
- 核心数值目标无法满足时，提供最小复现和准确限制；不得把无法完成写成已通过。
- 若需要改变数值契约、替换后端或大范围重构，由主代理形成具体方案和影响，再与用户沟通；例行修复无需反复请求确认。
- 不访问或修改仓库之外的用户文件，不做破坏性 Git 操作，不自动安装全局工具链。
- 若收到新的用户指令，主代理统一调整任务，通知受影响子代理，避免旧任务继续覆盖新决定。

## 12. 最终答复要求

最终用简体中文简洁报告：

1. 两个 P1 证据问题如何修正，实际数值结论是什么。
2. 是否发现生产代码缺陷，以及修复或限制。
3. 实际完成的自动化测试、CLI QA 与独立审核。
4. 修改文件及证据文档链接。
5. 尚未验证事项和下一轮任务：参数 DAG、结果表达式接入等。

不用代理数量或运行时长代替成果，不声称生产级、全面正确或跨平台通过，除非有相应证据。

## 13. 立即开始

现在请作为主代理执行：检查当前工作区与全局协作规范，建立文件所有权和依赖表，从空白上下文启动第一批只读取证代理，同时完成本地基线核查。随后按 Wave 1–3 组织并行实现、独立审核与最终交付。

请完成本轮授权范围内的工作，不仅提供方案。保持有意义的进度更新，直到全部验收通过，或给出经验证且无法在当前范围解决的阻塞与未完成清单。
